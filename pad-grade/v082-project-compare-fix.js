/* Pad Grade v0.8.2 comparison correction layer — retired by v0.8.3.
 *
 * v0.8.2 was linked directly from index.html and could participate in general
 * startup while the deferred feature chain was still resolving. Keep this file
 * as an inert compatibility target for cached v0.8.2 pages. The active correction
 * now loads through v078-dev.js only after the v0.8.1 comparison core and UI.
 */
(function retirePadGrade082ProjectComparisonFix(){
  'use strict';
  window.__padGradeRetiredV082='retired-startup-coupled-comparison-layer';
})();
