(()=>{
  function installTractorDataIntegrity(){
    if(window.__TRACTOR_DATA_INTEGRITY_INSTALLED)return true;if(typeof requireDB!=='function')return false;window.__TRACTOR_DATA_INTEGRITY_INSTALLED=true;const broken=new Map();
    const reqP=req=>new Promise((res,rej)=>{req.onsuccess=()=>res(req.result);req.onerror=()=>rej(req.error)});async function all(store){const d=requireDB(),tx=d.transaction(store,'readonly');return await reqP(tx.objectStore(store).getAll())}async function sessions(){return await all('sessions')}async function paths(){return await all('pathings')}const pathId=s=>s?.drivePathingId||s?.selectedPathingId||null;async function refs(id){return (await sessions()).filter(s=>pathId(s)===id)}async function getPath(id){const d=requireDB(),tx=d.transaction('pathings','readonly');return await reqP(tx.objectStore('pathings').get(id))}async function putPath(p){const d=requireDB(),tx=d.transaction('pathings','readwrite');tx.objectStore('pathings').put(p);await txDone(tx);return p}
    if(!window.__TRACTOR_PATH_DELETE_GUARD_INSTALLED&&window.IDBObjectStore){window.__TRACTOR_PATH_DELETE_GUARD_INSTALLED=true;window.__TRACTOR_PATH_DELETE_PERMIT=0;const nd=IDBObjectStore.prototype.delete,nc=IDBObjectStore.prototype.clear;window.__TRACTOR_NATIVE_IDB_DELETE=nd;window.__TRACTOR_NATIVE_IDB_CLEAR=nc;IDBObjectStore.prototype.delete=function(key){if((this.name==='pathings'||this.name==='pathSegments')&&!window.__TRACTOR_PATH_DELETE_PERMIT){const msg='This saved path is protected. Delete all drive sessions that use it before deleting the path.';console.warn(msg,{store:this.name,key});throw new DOMException(msg,'InvalidStateError')}return nd.call(this,key)};IDBObjectStore.prototype.clear=function(){if((this.name==='pathings'||this.name==='pathSegments')&&!window.__TRACTOR_PATH_DELETE_PERMIT)throw new DOMException('Saved path storage is protected.','InvalidStateError');return nc.call(this)}}
    async function archivePath(id){const p=await getPath(id);if(!p)throw Error('Path no longer exists.');p.archived=true;p.archivedAt=Date.now();return await putPath(p)}async function recoverPath(id){const p=await getPath(id);if(!p)throw Error('Path no longer exists.');delete p.archived;delete p.archivedAt;return await putPath(p)}
    async function guardedDeletePath(id){const used=await refs(id);if(used.length)throw Error(`Protected by ${used.length} drive session${used.length===1?'':'s'}. Delete those drives first.`);const d=requireDB(),tx=d.transaction(['pathings','pathSegments'],'readwrite');window.__TRACTOR_PATH_DELETE_PERMIT++;try{tx.objectStore('pathings').delete(id);const idx=tx.objectStore('pathSegments').index('byPathing'),keys=await reqP(idx.getAllKeys(IDBKeyRange.only(id)));for(const k of keys)tx.objectStore('pathSegments').delete(k);await txDone(tx)}finally{window.__TRACTOR_PATH_DELETE_PERMIT=Math.max(0,(window.__TRACTOR_PATH_DELETE_PERMIT||1)-1)}}
    async function deleteDrive(id){const d=requireDB(),tx=d.transaction(['sessions','points'],'readwrite');tx.objectStore('sessions').delete(id);const idx=tx.objectStore('points').index('bySession'),keys=await reqP(idx.getAllKeys(IDBKeyRange.only(id)));for(const k of keys)tx.objectStore('points').delete(k);await txDone(tx);broken.delete(id)}
    async function scan(){broken.clear();const [ss,pp]=await Promise.all([sessions(),paths()]),ids=new Set(pp.map(p=>p.id));for(const s of ss){const id=pathId(s);if(id&&!ids.has(id))broken.set(s.id,{session:s,missingPathId:id})}refreshBrokenDialog();return broken}
    const dlg=document.createElement('dialog');dlg.innerHTML='<div class="modal"><h2>Drive Data Integrity</h2><div class="small">These drive records reference paths that no longer exist and cannot be loaded safely.</div><div id="integrityList"></div><div class="modalActions"><button id="integrityClose">Close</button></div></div>';document.body.appendChild(dlg);dlg.querySelector('#integrityClose').onclick=()=>dlg.close();function refreshBrokenDialog(){const h=dlg.querySelector('#integrityList');h.replaceChildren();for(const {session:s,missingPathId:id} of broken.values()){const r=document.createElement('div');r.className='integrityBad';r.innerHTML=`<b>${escapeHTML(s?.driveName||s?.settings?.sessionName||s?.drivePathingName||s?.selectedPathingName||'Drive')}</b><div class="small">Missing path ${escapeHTML(id)}</div>`;const b=document.createElement('button');b.className='danger';b.textContent='Delete broken drive';b.onclick=async()=>{if(confirm('Permanently delete this broken drive and its GPS points?')){await deleteDrive(s.id);await scan();await refreshPathLibrary()}};r.appendChild(b);h.appendChild(r)}}
    const pdlg=document.createElement('dialog');pdlg.id='safePathDlg';pdlg.innerHTML='<div class="modal"><div class="statusRow"><div><h2>Saved Paths</h2><div class="small">A path used by any drive is protected. Load changes the PLAN selection only; it never changes an existing drive.</div></div><button id="safePathClose">Close</button></div><div id="safePathList"></div></div>';document.body.appendChild(pdlg);pdlg.querySelector('#safePathClose').onclick=()=>pdlg.close();const ph=pdlg.querySelector('#safePathList');
    async function refreshPathLibrary(){const [pp,ss]=await Promise.all([paths(),sessions()]),by=new Map();for(const s of ss){const id=pathId(s);if(!id)continue;if(!by.has(id))by.set(id,[]);by.get(id).push(s)}pp.sort((a,b)=>Boolean(a.archived)-Boolean(b.archived)||(b.updatedAt||0)-(a.updatedAt||0));ph.replaceChildren();for(const p of pp){const used=by.get(p.id)||[],r=document.createElement('div');r.className='pathSafeItem'+(p.archived?' pathArchived':'');r.innerHTML=`<div class="pathSafeTop"><div><b>${escapeHTML(p.name||p.pathingName||'Saved path')}</b><div class="small">${p.archived?'ARCHIVED • ':''}${used.length?'PROTECTED':'Available'}</div></div><span class="badge">${used.length} drive${used.length===1?'':'s'}</span></div>`;if(used.length){const dep=document.createElement('div');dep.className='small';dep.style.marginTop='5px';dep.textContent='Protected by: '+used.map(s=>s?.driveName||s?.settings?.sessionName||s?.settings?.operation||s.id).join(', ');r.appendChild(dep)}const load=document.createElement('button');load.className='primary';load.textContent='Load in PLAN';load.disabled=!!p.archived;load.onclick=async()=>{try{if(!window.TractorDriveState?.loadPlanPath)throw Error('PLAN path loader is not ready.');await window.TractorDriveState.loadPlanPath(p.id);pdlg.close()}catch(e){alert('Could not load path: '+(e?.message||e))}};const a=document.createElement('button');a.textContent=p.archived?'Recover':'Archive';a.onclick=async()=>{p.archived?await recoverPath(p.id):await archivePath(p.id);await refreshPathLibrary()};r.append(load,a);if(used.length){const protect=document.createElement('button');protect.textContent='Protected';protect.disabled=true;r.appendChild(protect)}else{const d=document.createElement('button');d.className='danger';d.textContent='Delete';d.onclick=async()=>{if(confirm('Permanently delete this unreferenced path?')){try{await guardedDeletePath(p.id);await refreshPathLibrary()}catch(e){alert(e.message)}}};r.appendChild(d)}ph.appendChild(r)}protectLegacyDialogs(by)}
    function protectLegacyDialogs(by){for(const dialog of document.querySelectorAll('dialog')){if(dialog===pdlg||dialog===dlg)continue;for(const row of dialog.querySelectorAll('[data-pathing-id],[data-id],.pathingItem,.savedPathItem')){const id=row.dataset?.pathingId||row.dataset?.id;if(!id||!by.has(id))continue;for(const b of row.querySelectorAll('button')){if(/delete|remove/i.test(b.textContent||'')){b.disabled=true;b.textContent='Protected';b.classList.remove('danger')}}}}}
    function openPathLibrary(){refreshPathLibrary();pdlg.showModal()}for(const id of ['pathingsBtn','openPathingsBtn']){const b=document.getElementById(id);if(b)b.onclick=openPathLibrary}
    const observer=new MutationObserver(()=>{if(pdlg.open)return;refreshPathLibrary().catch(()=>{})});observer.observe(document.body,{childList:true,subtree:true});
    const st=document.createElement('style');st.textContent='.integrityBad,.pathSafeItem{border:1px solid var(--line);border-radius:10px;padding:9px;margin:7px 0}.pathSafeTop{display:flex;justify-content:space-between}.pathArchived{opacity:.7}.pathSafeItem>button{margin:7px 5px 0 0}';document.head.appendChild(st);window.TractorIntegrity={scan,isBroken:id=>broken.has(id),broken,paths,sessions,refs,archivePath,recoverPath,deletePath:guardedDeletePath,deleteDrive,openPathLibrary,refreshPathLibrary};setTimeout(async()=>{await scan();await refreshPathLibrary();if(broken.size)dlg.showModal()},500);return true
  }window.installTractorDataIntegrity=installTractorDataIntegrity;
})();