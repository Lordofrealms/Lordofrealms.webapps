name: Pad Grade Android

on:
  push:
    branches: [main, pad-grade-dev]
    paths:
      - 'pad-grade/**'
      - 'pad-grade-android/**'
      - '.github/workflows/pad-grade-android.yml'
  pull_request:
    branches: [main, pad-grade-dev]
    paths:
      - 'pad-grade/**'
      - 'pad-grade-android/**'
      - '.github/workflows/pad-grade-android.yml'
  workflow_dispatch:

concurrency:
  group: pad-grade-android-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Validate web scripts
        run: |
          node --check pad-grade/capture-fix.js
          node --check pad-grade/map-instance-hook-v064.js
          node --check pad-grade/v030.js
          node --check pad-grade/v031.js
          node --check pad-grade/v040.js
          node --check pad-grade/v040-sync.js
          node --check pad-grade/v041.js
          node --check pad-grade/v041-persist.js
          node --check pad-grade/v042.js
          node --check pad-grade/v043.js
          node --check pad-grade/v046.js
          node --check pad-grade/v047.js
          node --check pad-grade/v048.js
          node --check pad-grade/v049.js
          node --check pad-grade/v050.js
          node --check pad-grade/v051.js
          node --check pad-grade/v052.js
          node --check pad-grade/v053.js
          node --check pad-grade/v060-dev.js
          node --check pad-grade/v061-dev.js
          node --check pad-grade/v062-dev.js
          node --check pad-grade/v063-dev.js
          node --check pad-grade/v064-dev.js
          node --check pad-grade/v065-dev.js
          node --check pad-grade/v066-dev.js
          node --check pad-grade/v067-dev.js
          node --check pad-grade/v069-dev.js
          node --check pad-grade/v070-dev.js
          node --check pad-grade/dev-selftest.js
          node --check pad-grade/migration-core.js
          node --check pad-grade/grid-core.js

      - name: Verify v0.7.0 stability and map-feature gates
        run: |
          grep -F "padGradeHeatmapOptInV061" pad-grade/v062-dev.js
          grep -F "platform.precisionGeolocation" pad-grade/platform.js
          grep -F "dev-v0.6.1-stability" pad-grade/v062-dev.js
          grep -F 'src="map-instance-hook-v064.js' pad-grade/index.html
          grep -F 'src="v063-dev.js' pad-grade/index.html
          grep -F 'src="v064-dev.js' pad-grade/index.html
          grep -F 'src="v065-dev.js' pad-grade/index.html
          grep -F 'src="v067-dev.js' pad-grade/index.html
          grep -F "window.__padGradeMapInstance=this" pad-grade/map-instance-hook-v064.js
          grep -F "type:'geojson'" pad-grade/v063-dev.js
          grep -F "type:'MultiPolygon'" pad-grade/v063-dev.js
          grep -F "fill-antialias':false" pad-grade/v063-dev.js
          grep -F "window.__padGradeHeatmapLocation='gps-map-geojson-mesh'" pad-grade/v063-dev.js
          grep -F "window.__padGradeHeatmapResolution='adaptive-304-512-long-axis-grouped-multipolygon'" pad-grade/v063-dev.js
          grep -F "window.__padGradeLaserPlacementLocation='gps-map'" pad-grade/v063-dev.js
          grep -F "window.__padGradeNotesUi='top-level-dialog'" pad-grade/v064-dev.js
          grep -F "window.__padGradeMapControlsLocation='below-map-key'" pad-grade/v065-dev.js
          grep -F "v070-dev.js" pad-grade/v065-dev.js
          grep -F "window.__padGradeHeatmapUiV066=true" pad-grade/v066-dev.js
          grep -F "Green is categorical" pad-grade/v067-dev.js
          grep -F "#4f8f3a 46%,#4f8f3a 54%" pad-grade/v067-dev.js
          grep -F "window.__padGradeCategoricalGradeV067=true" pad-grade/v067-dev.js
          grep -F "background-index-first-read-settings-then-reload" pad-grade/v070-dev.js
          grep -F "completeProjectFolderRecovery" pad-grade/v070-dev.js
          grep -F "magnitude-aware IDW² zero crossing" pad-grade/dev-selftest.js

      - name: Run Pad Grade dev self-tests
        run: node pad-grade/dev-selftest.js

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - name: Install Android SDK
        run: |
          sdkmanager 'platforms;android-36' 'build-tools;36.0.0'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'

      - name: Resolve app version and channel
        run: |
          VERSION=$(sed -n 's/.*versionName = "\([^"]*\)".*/\1/p' pad-grade-android/app/build.gradle.kts | head -1)
          test -n "$VERSION"
          BRANCH="${GITHUB_HEAD_REF:-$GITHUB_REF_NAME}"
          if [ "$BRANCH" = "main" ]; then
            CHANNEL=stable
            RELEASE_TAG="pad-grade-v$VERSION"
          elif [ "$BRANCH" = "pad-grade-dev" ]; then
            CHANNEL=dev
            RELEASE_TAG="pad-grade-dev-v$VERSION"
          else
            CHANNEL=pr
            RELEASE_TAG="pad-grade-pr-v$VERSION"
          fi
          echo "APP_VERSION=$VERSION" >> "$GITHUB_ENV"
          echo "BUILD_CHANNEL=$CHANNEL" >> "$GITHUB_ENV"
          echo "APK_NAME=Pad-Grade-v$VERSION.apk" >> "$GITHUB_ENV"
          echo "ARTIFACT_NAME=pad-grade-$CHANNEL" >> "$GITHUB_ENV"
          echo "RELEASE_TAG=$RELEASE_TAG" >> "$GITHUB_ENV"

      - name: Build debug APK
        run: gradle -p pad-grade-android :app:assembleDebug --stacktrace

      - name: Verify canonical web assets packaged
        run: |
          APK=pad-grade-android/app/build/outputs/apk/debug/app-debug.apk
          unzip -l "$APK" | grep 'assets/index.html'
          unzip -l "$APK" | grep 'assets/platform.js'
          unzip -l "$APK" | grep 'assets/v040.js'
          unzip -l "$APK" | grep 'assets/v040-sync.js'
          unzip -l "$APK" | grep 'assets/v040.css'
          unzip -l "$APK" | grep 'assets/v041.js'
          unzip -l "$APK" | grep 'assets/v041-persist.js'
          unzip -l "$APK" | grep 'assets/v041.css'
          unzip -l "$APK" | grep 'assets/v042.js'
          unzip -l "$APK" | grep 'assets/v042.css'
          unzip -l "$APK" | grep 'assets/v048.js'
          unzip -l "$APK" | grep 'assets/v052.js'
          unzip -l "$APK" | grep 'assets/migration-core.js'
          unzip -l "$APK" | grep 'assets/grid-core.js'
          unzip -l "$APK" | grep 'assets/map-instance-hook-v064.js'
          if [ -f pad-grade/v060-dev.js ]; then unzip -l "$APK" | grep 'assets/v060-dev.js'; unzip -l "$APK" | grep 'assets/v060-dev.css'; fi
          if [ -f pad-grade/v061-dev.js ]; then unzip -l "$APK" | grep 'assets/v061-dev.js'; unzip -l "$APK" | grep 'assets/v061-dev.css'; fi
          if [ -f pad-grade/v062-dev.js ]; then unzip -l "$APK" | grep 'assets/v062-dev.js'; fi
          if [ -f pad-grade/v063-dev.js ]; then unzip -l "$APK" | grep 'assets/v063-dev.js'; fi
          if [ -f pad-grade/v064-dev.js ]; then unzip -l "$APK" | grep 'assets/v064-dev.js'; fi
          if [ -f pad-grade/v065-dev.js ]; then unzip -l "$APK" | grep 'assets/v065-dev.js'; fi
          if [ -f pad-grade/v066-dev.js ]; then unzip -l "$APK" | grep 'assets/v066-dev.js'; fi
          if [ -f pad-grade/v067-dev.js ]; then unzip -l "$APK" | grep 'assets/v067-dev.js'; fi
          if [ -f pad-grade/v069-dev.js ]; then unzip -l "$APK" | grep 'assets/v069-dev.js'; fi
          if [ -f pad-grade/v070-dev.js ]; then unzip -l "$APK" | grep 'assets/v070-dev.js'; fi
          unzip -p "$APK" assets/index.html | grep -F 'src="map-instance-hook-v064.js'
          unzip -p "$APK" assets/index.html | grep -F 'src="v063-dev.js'
          unzip -p "$APK" assets/index.html | grep -F 'src="v064-dev.js'
          unzip -p "$APK" assets/index.html | grep -F 'src="v065-dev.js'
          unzip -p "$APK" assets/index.html | grep -F 'src="v067-dev.js'
          unzip -p "$APK" assets/v063-dev.js | grep -F "gps-map-geojson-mesh"
          unzip -p "$APK" assets/v063-dev.js | grep -F "adaptive-304-512-long-axis-grouped-multipolygon"
          unzip -p "$APK" assets/v067-dev.js | grep -F "Green is categorical"
          unzip -p "$APK" assets/v070-dev.js | grep -F "background-index-first-read-settings-then-reload"

      - name: Verify dev package, launcher icon, and WebP integrity
        run: |
          APK=pad-grade-android/app/build/outputs/apk/debug/app-debug.apk
          AAPT="$ANDROID_HOME/build-tools/36.0.0/aapt"
          if [ "$BUILD_CHANNEL" = "dev" ]; then
            "$AAPT" dump badging "$APK" | grep "package: name='com.lordofrealms.padgrade.dev'"
            "$AAPT" dump badging "$APK" | grep "application-label:'Pad Grade DEV'"
          fi
          unzip -l "$APK" | grep 'res/mipmap-nodpi-v4/ic_launcher.webp'
          unzip -l "$APK" | grep 'res/mipmap-nodpi-v4/ic_launcher_round.webp'
          if unzip -l "$APK" | grep -q 'res/mipmap-anydpi-v26/ic_launcher.xml'; then
            echo 'Unexpected adaptive launcher override remains in APK' >&2
            exit 1
          fi
          "$AAPT" dump badging "$APK" | grep "application-icon" | grep 'ic_launcher'
          unzip -p "$APK" res/mipmap-nodpi-v4/ic_launcher.webp > /tmp/ic_launcher.webp
          unzip -p "$APK" res/mipmap-nodpi-v4/ic_launcher_round.webp > /tmp/ic_launcher_round.webp
          python3 - <<'PY'
          from pathlib import Path
          for name in ('/tmp/ic_launcher.webp', '/tmp/ic_launcher_round.webp'):
              data = Path(name).read_bytes()
              assert data[:4] == b'RIFF', f'{name}: missing RIFF header'
              assert data[8:12] == b'WEBP', f'{name}: missing WEBP signature'
              declared = int.from_bytes(data[4:8], 'little') + 8
              actual = len(data)
              assert declared == actual, f'{name}: truncated WebP: declared {declared}, actual {actual}'
              assert actual > 4000, f'{name}: unexpectedly small launcher file: {actual}'
              print(f'{name}: valid WebP, {actual} bytes')
          PY

      - name: Name APK
        run: cp pad-grade-android/app/build/outputs/apk/debug/app-debug.apk "$APK_NAME"

      - name: Upload APK artifact
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.APK_NAME }}

      - name: Publish stable GitHub Release APK
        if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release edit "$RELEASE_TAG" --title "Pad Grade v$APP_VERSION" --notes "Validated Pad Grade Android v$APP_VERSION stable build from the main branch. This is the version intended for normal use."
            gh release upload "$RELEASE_TAG" "$APK_NAME" --clobber
          else
            gh release create "$RELEASE_TAG" "$APK_NAME" \
              --target "$GITHUB_SHA" \
              --title "Pad Grade v$APP_VERSION" \
              --notes "Validated Pad Grade Android v$APP_VERSION stable build from the main branch. This is the version intended for normal use."
          fi

      - name: Publish dev GitHub Pre-release APK
        if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/pad-grade-dev'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          NOTES="DEVELOPMENT / TEST BUILD from the pad-grade-dev branch. Built from commit $GITHUB_SHA. Installs separately as Pad Grade DEV so it does not replace the stable app or its local data. The bottom survey grid remains discrete/authoritative. Interpolated magnitude-aware IDW² shading exists only on the calibrated GPS map, defaults ON, and now uses roughly ten times the previous polygon density with grouped MultiPolygon features. Heatmap transparency is adjustable in Settings and there is a quick map toggle. Cut and fill use independently scaled spectra, while green is reserved exclusively for the configured on-grade tolerance band. Durable settings recovery now indexes a reselected SAF folder once and restores the saved settings/last project before writes are re-enabled. Laser placement is performed on the GPS map, with its controls below the map/key. This APK may contain incomplete or experimental features and is not the stable release. The main branch remains the last known-good Pad Grade build."
          if gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release edit "$RELEASE_TAG" --title "Pad Grade v$APP_VERSION — DEV BUILD" --notes "$NOTES" --prerelease
            gh release upload "$RELEASE_TAG" "$APK_NAME" --clobber
          else
            gh release create "$RELEASE_TAG" "$APK_NAME" \
              --target "$GITHUB_SHA" \
              --title "Pad Grade v$APP_VERSION — DEV BUILD" \
              --notes "$NOTES" \
              --prerelease
          fi

      - name: Verify stable GitHub Release APK
        if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/main'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release view "$RELEASE_TAG" --json assets --jq '.assets[].name' | grep -Fx "$APK_NAME"

      - name: Verify dev GitHub Pre-release APK
        if: (github.event_name == 'push' || github.event_name == 'workflow_dispatch') && github.ref == 'refs/heads/pad-grade-dev'
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          test "$(gh release view "$RELEASE_TAG" --json isPrerelease --jq '.isPrerelease')" = "true"
          gh release view "$RELEASE_TAG" --json assets --jq '.assets[].name' | grep -Fx "$APK_NAME"
