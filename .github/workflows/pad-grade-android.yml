name: Pad Grade Android

on:
  push:
    branches: [pad-grade-android-wrapper]
    paths:
      - 'pad-grade-android/ci-build-trigger.txt'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - uses: android-actions/setup-android@v3

      - name: Install Android SDK
        run: |
          sdkmanager 'platforms;android-36' 'build-tools;36.0.0'

      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'

      - name: Build debug APK
        run: gradle -p pad-grade-android :app:assembleDebug --stacktrace

      - name: Verify canonical web assets packaged
        run: |
          unzip -l pad-grade-android/app/build/outputs/apk/debug/app-debug.apk | grep 'assets/pad-grade/index.html'
          unzip -l pad-grade-android/app/build/outputs/apk/debug/app-debug.apk | grep 'assets/pad-grade/platform.js'

      - name: Upload debug APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: pad-grade-debug
          path: pad-grade-android/app/build/outputs/apk/debug/app-debug.apk
