name: Battery Monitor Signed Firmware Release

on:
  workflow_dispatch:
    inputs:
      source_sha:
        description: 'Exact 40-character commit SHA to promote/sign'
        required: true
        type: string
      ci_run_id:
        description: 'Successful Battery Monitor Toolchain run ID containing the exact firmware bytes to promote'
        required: true
        type: string
      version:
        description: 'Exact firmware version from battery-monitor/firmware/idf/version.txt'
        required: true
        default: '0.1.0.11'
        type: string

permissions:
  contents: read
  actions: read

jobs:
  signed_firmware:
    name: Promote and sign exact CI ESP32 firmware
    runs-on: ubuntu-latest
    environment: battery-monitor-production-signing
    env:
      BATMON_FIRMWARE_SIGNING_KEY_B64: ${{ secrets.BATMON_FIRMWARE_SIGNING_KEY_B64 }}
      BATMON_FIRMWARE_SIGNING_KEY_PASSWORD: ${{ secrets.BATMON_FIRMWARE_SIGNING_KEY_PASSWORD }}
      EXPECTED_KEY_FINGERPRINT: 69d6d94b706c57e783c6e2e4ad17e781e84d1e4e32addbcfca68976483be5e6e
    steps:
      - name: Validate requested source SHA and CI run ID
        shell: bash
        run: |
          set -euo pipefail
          if [[ ! '${{ inputs.source_sha }}' =~ ^[0-9a-fA-F]{40}$ ]]; then
            echo 'source_sha must be an exact 40-character commit SHA.' >&2
            exit 1
          fi
          if [[ ! '${{ inputs.ci_run_id }}' =~ ^[0-9]+$ ]]; then
            echo 'ci_run_id must be a numeric GitHub Actions run ID.' >&2
            exit 1
          fi

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ inputs.source_sha }}
          persist-credentials: false

      - name: Validate authoritative release version
        shell: bash
        run: |
          set -euo pipefail
          version_file='battery-monitor/firmware/idf/version.txt'
          test -f "$version_file" || { echo 'Authoritative firmware version.txt is missing.' >&2; exit 1; }
          source_version="$(tr -d '\r\n' < "$version_file")"
          if [[ "$source_version" != '${{ inputs.version }}' ]]; then
            echo "Requested release version '${{ inputs.version }}' does not match authoritative source version '$source_version'." >&2
            exit 1
          fi
          if [[ ! "$source_version" =~ ^([0-9]+)\.([0-9]+)\.([0-9]+)\.([1-9][0-9]*)$ ]]; then
            echo "Authoritative version must be major.minor.patch.release_sequence: $source_version" >&2
            exit 1
          fi
          release_sequence="${BASH_REMATCH[4]}"
          if (( release_sequence > 4294967295 )); then
            echo "Release sequence exceeds uint32 range: $release_sequence" >&2
            exit 1
          fi
          echo "BATMON_RELEASE_VERSION=$source_version" >> "$GITHUB_ENV"
          echo "BATMON_RELEASE_SEQUENCE=$release_sequence" >> "$GITHUB_ENV"

      - name: Validate exact CI run authority
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
          CI_RUN_ID: ${{ inputs.ci_run_id }}
          EXPECTED_SOURCE_SHA: ${{ inputs.source_sha }}
        run: |
          set -euo pipefail
          api="https://api.github.com/repos/${GITHUB_REPOSITORY}/actions/runs/${CI_RUN_ID}"
          run_json="$(curl --fail --silent --show-error -L \
            -H 'Accept: application/vnd.github+json' \
            -H "Authorization: Bearer ${GH_TOKEN}" \
            -H 'X-GitHub-Api-Version: 2022-11-28' \
            "$api")"
          actual_sha="$(jq -r '.head_sha' <<<"$run_json")"
          conclusion="$(jq -r '.conclusion' <<<"$run_json")"
          workflow_path="$(jq -r '.path' <<<"$run_json")"
          event="$(jq -r '.event' <<<"$run_json")"
          if [[ "$actual_sha" != "$EXPECTED_SOURCE_SHA" ]]; then
            echo "CI run head SHA '$actual_sha' does not match requested source '$EXPECTED_SOURCE_SHA'." >&2
            exit 1
          fi
          if [[ "$conclusion" != 'success' ]]; then
            echo "CI run must be completed successfully; conclusion was '$conclusion'." >&2
            exit 1
          fi
          if [[ "$workflow_path" != '.github/workflows/battery-monitor-ci.yml' ]]; then
            echo "CI run came from unexpected workflow '$workflow_path'." >&2
            exit 1
          fi
          if [[ "$event" != 'push' && "$event" != 'workflow_dispatch' ]]; then
            echo "CI run event '$event' is not an accepted release-promotion source." >&2
            exit 1
          fi
          echo "Promoting exact firmware bytes from CI run ${CI_RUN_ID}, source ${actual_sha}."

      - name: Download exact CI firmware artifact
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: battery-monitor-esp32-CI-UNSIGNED-v0.1.0
          path: /tmp/battery-monitor-signed-firmware
          github-token: ${{ github.token }}
          repository: ${{ github.repository }}
          run-id: ${{ inputs.ci_run_id }}

      - name: Validate promoted firmware authority and hashes
        shell: bash
        run: |
          set -euo pipefail
          dir=/tmp/battery-monitor-signed-firmware
          test -f "$dir/BatteryMonitor.ino.bin"
          test -f "$dir/BatteryMonitor.ino.merged.bin"
          test -f "$dir/version.txt"
          test -f "$dir/BUILD_AUTHORITY.txt"
          test "$(stat -c %s "$dir/BatteryMonitor.ino.merged.bin")" -eq 4194304
          test "$(tr -d '\r\n' < "$dir/version.txt")" = "$BATMON_RELEASE_VERSION"
          grep -qx "app_version=$BATMON_RELEASE_VERSION" "$dir/BUILD_AUTHORITY.txt"
          grep -qx "software_release_sequence=$BATMON_RELEASE_SEQUENCE" "$dir/BUILD_AUTHORITY.txt"
          grep -qx 'software_signed_release_floor=encrypted-nvs-strictly-newer' "$dir/BUILD_AUTHORITY.txt"
          grep -qx 'post_boot_ota_rollback=enabled' "$dir/BUILD_AUTHORITY.txt"
          grep -qx 'hardware_efuse_app_anti_rollback=disabled-pending-secure-boot-production-efuse-gate' "$dir/BUILD_AUTHORITY.txt"
          grep -qx 'http_server=esp_http_server' "$dir/BUILD_AUTHORITY.txt"
          grep -qx 'http_transport=native-esp-idf' "$dir/BUILD_AUTHORITY.txt"
          grep -qx 'http_max_client_sessions=15' "$dir/BUILD_AUTHORITY.txt"
          grep -qx 'http_max_client_sessions_runtime_configurable=yes' "$dir/BUILD_AUTHORITY.txt"
          grep -qx 'http_tcp_sndbuf_default=12288' "$dir/BUILD_AUTHORITY.txt"
          grep -qx 'lwip_max_sockets=30' "$dir/BUILD_AUTHORITY.txt"
          expected_app="$(awk -F= '$1=="application_sha256"{print $2}' "$dir/BUILD_AUTHORITY.txt")"
          expected_merged="$(awk -F= '$1=="merged_sha256"{print $2}' "$dir/BUILD_AUTHORITY.txt")"
          actual_app="$(sha256sum "$dir/BatteryMonitor.ino.bin" | awk '{print $1}')"
          actual_merged="$(sha256sum "$dir/BatteryMonitor.ino.merged.bin" | awk '{print $1}')"
          test -n "$expected_app" && test "$actual_app" = "$expected_app"
          test -n "$expected_merged" && test "$actual_merged" = "$expected_merged"
          echo "Exact promoted app SHA-256: $actual_app"
          echo "Exact promoted merged SHA-256: $actual_merged"

      - name: Sign and independently verify promoted firmware
        shell: bash
        run: |
          set -euo pipefail
          test -n "$BATMON_FIRMWARE_SIGNING_KEY_B64" || { echo 'BATMON_FIRMWARE_SIGNING_KEY_B64 secret is not configured.' >&2; exit 1; }
          test -n "$BATMON_FIRMWARE_SIGNING_KEY_PASSWORD" || { echo 'BATMON_FIRMWARE_SIGNING_KEY_PASSWORD secret is not configured.' >&2; exit 1; }

          key=/tmp/batmon-firmware-signing-key.pem
          trap 'rm -f "$key" /tmp/batmon-wrong-key.pem /tmp/batmon-wrong.sig /tmp/batmon-tampered.bin' EXIT
          printf '%s' "$BATMON_FIRMWARE_SIGNING_KEY_B64" | base64 --decode > "$key"
          chmod 600 "$key"

          actual_fingerprint=$(openssl pkey \
            -in "$key" \
            -passin env:BATMON_FIRMWARE_SIGNING_KEY_PASSWORD \
            -pubout -outform DER | sha256sum | awk '{print $1}')
          if [[ "$actual_fingerprint" != "$EXPECTED_KEY_FINGERPRINT" ]]; then
            echo "Signing key fingerprint mismatch: $actual_fingerprint" >&2
            exit 1
          fi

          for image in BatteryMonitor.ino.bin BatteryMonitor.ino.merged.bin; do
            file="/tmp/battery-monitor-signed-firmware/$image"
            openssl dgst -sha256 \
              -sign "$key" \
              -passin env:BATMON_FIRMWARE_SIGNING_KEY_PASSWORD \
              -sigopt rsa_padding_mode:pss \
              -sigopt rsa_pss_saltlen:-1 \
              -out "$file.sig" \
              "$file"
            test "$(wc -c < "$file.sig")" -eq 384
            openssl dgst -sha256 \
              -verify battery-monitor/signing/battery_monitor_secureboot_rsa3072_public.pem \
              -signature "$file.sig" \
              -sigopt rsa_padding_mode:pss \
              -sigopt rsa_pss_saltlen:-1 \
              "$file"
          done

          cp /tmp/battery-monitor-signed-firmware/BatteryMonitor.ino.bin /tmp/batmon-tampered.bin
          python - <<'PY'
          p='/tmp/batmon-tampered.bin'
          data=bytearray(open(p,'rb').read())
          data[len(data)//2] ^= 1
          open(p,'wb').write(data)
          PY
          if openssl dgst -sha256 \
              -verify battery-monitor/signing/battery_monitor_secureboot_rsa3072_public.pem \
              -signature /tmp/battery-monitor-signed-firmware/BatteryMonitor.ino.bin.sig \
              -sigopt rsa_padding_mode:pss \
              -sigopt rsa_pss_saltlen:-1 \
              /tmp/batmon-tampered.bin; then
            echo 'Tampered firmware unexpectedly verified.' >&2
            exit 1
          fi

          openssl genpkey -algorithm RSA -pkeyopt rsa_keygen_bits:3072 -out /tmp/batmon-wrong-key.pem >/dev/null 2>&1
          openssl dgst -sha256 \
            -sign /tmp/batmon-wrong-key.pem \
            -sigopt rsa_padding_mode:pss \
            -sigopt rsa_pss_saltlen:-1 \
            -out /tmp/batmon-wrong.sig \
            /tmp/battery-monitor-signed-firmware/BatteryMonitor.ino.bin
          if openssl dgst -sha256 \
              -verify battery-monitor/signing/battery_monitor_secureboot_rsa3072_public.pem \
              -signature /tmp/batmon-wrong.sig \
              -sigopt rsa_padding_mode:pss \
              -sigopt rsa_pss_saltlen:-1 \
              /tmp/battery-monitor-signed-firmware/BatteryMonitor.ino.bin; then
            echo 'Firmware signed by a different key unexpectedly verified.' >&2
            exit 1
          fi

          {
            echo 'schema=BATMON_SIGNED_RELEASE_V1'
            echo 'promotion_mode=exact-ci-artifact'
            echo 'promoted_ci_run_id=${{ inputs.ci_run_id }}'
            echo 'source_sha=${{ inputs.source_sha }}'
            echo "version=$BATMON_RELEASE_VERSION"
            echo "software_release_sequence=$BATMON_RELEASE_SEQUENCE"
            echo 'software_signed_release_floor=encrypted-nvs-strictly-newer'
            echo 'post_boot_ota_rollback=enabled'
            echo 'hardware_efuse_app_anti_rollback=disabled-pending-secure-boot-production-efuse-gate'
            echo 'signed_usb_ota=SIGNED_USB_OTA_V1'
            echo 'signed_lan_ota=SIGNED_LAN_OTA_V1'
            echo 'architecture=ESP-IDF+Arduino-component'
            echo 'esp_idf_version=5.5.5'
            echo 'esp_idf_commit=b774170ff46c393eeb5e495ea37936038d3f4f4f'
            echo 'arduino_esp32_base_release=3.3.11'
            echo 'arduino_esp32_component_source=upstream-git'
            echo 'arduino_esp32_commit=5cdf8975ae8d9e35888b724b01a444d22406424e'
            echo 'http_server=esp_http_server'
            echo 'http_transport=native-esp-idf'
            echo 'http_max_client_sessions=15'
            echo 'http_max_client_sessions_runtime_configurable=yes'
            echo 'http_tcp_sndbuf_default=12288'
            echo 'lwip_max_sockets=30'
            echo 'algorithm=RSA-3072-PSS-SHA256'
            echo "public_key_spki_sha256=$EXPECTED_KEY_FINGERPRINT"
            sha256sum /tmp/battery-monitor-signed-firmware/BatteryMonitor.ino.bin
            sha256sum /tmp/battery-monitor-signed-firmware/BatteryMonitor.ino.merged.bin
          } > /tmp/battery-monitor-signed-firmware/SIGNED_RELEASE.txt

      - name: Upload signed firmware
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: Battery-Monitor-Signed-Firmware-${{ inputs.version }}
          path: /tmp/battery-monitor-signed-firmware

  windows_signed:
    name: Build separate signed customer and Factory Service packages
    runs-on: windows-latest
    needs: signed_firmware
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ inputs.source_sha }}
          persist-credentials: false

      - uses: actions/setup-dotnet@a98b56852c35b8e3190ac28c8c2271da59106c68 # v6
        with:
          dotnet-version: '8.0.x'

      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1 # v6
        with:
          python-version: '3.12'

      - name: Download exact signed firmware
        uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8
        with:
          name: Battery-Monitor-Signed-Firmware-${{ inputs.version }}
          path: battery-monitor/windows/staging/firmware

      - name: Download and verify Espressif esptool 5.3.1
        shell: pwsh
        run: |
          $zip = Join-Path $env:RUNNER_TEMP 'esptool-v5.3.1-windows-amd64.zip'
          Invoke-WebRequest -Uri 'https://github.com/espressif/esptool/releases/download/v5.3.1/esptool-v5.3.1-windows-amd64.zip' -OutFile $zip
          $actual = (Get-FileHash $zip -Algorithm SHA256).Hash.ToLowerInvariant()
          $expected = '2b4a73c45db27426685896f64ce3e557f63a64f43cc100cb65c0cc3486af96d3'
          if ($actual -ne $expected) { throw "esptool SHA256 mismatch: $actual" }
          New-Item -ItemType Directory -Force -Path battery-monitor/windows/staging/esptool | Out-Null
          Expand-Archive -Path $zip -DestinationPath battery-monitor/windows/staging/esptool -Force
          $exe = Get-ChildItem battery-monitor/windows/staging/esptool -Filter esptool.exe -Recurse | Select-Object -First 1
          if (-not $exe) { throw 'esptool.exe not found after extraction' }
          & $exe.FullName version

      - name: Build pinned Espressif Security 2 provisioner helper
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $vendorCommit = '3ba4a43776a737054b43ead300ea0377787cad4a'
          $idfCommit = 'fcae32885b0296b32044cb99ecbdc50d98dddb83'
          $vendor = Join-Path $env:RUNNER_TEMP 'idf-extra-components'
          New-Item -ItemType Directory -Force -Path $vendor | Out-Null
          git -C $vendor init
          git -C $vendor remote add origin https://github.com/espressif/idf-extra-components.git
          git -C $vendor fetch --depth=1 origin $vendorCommit
          git -C $vendor checkout --detach FETCH_HEAD
          if ((git -C $vendor rev-parse HEAD).Trim() -ne $vendorCommit) { throw 'Espressif provisioner source commit mismatch' }
          $idf = Join-Path $env:RUNNER_TEMP 'esp-idf'
          New-Item -ItemType Directory -Force -Path $idf | Out-Null
          git -C $idf init
          git -C $idf remote add origin https://github.com/espressif/esp-idf.git
          git -C $idf fetch --depth=1 origin $idfCommit
          git -C $idf checkout --detach FETCH_HEAD
          if ((git -C $idf rev-parse HEAD).Trim() -ne $idfCommit) { throw 'ESP-IDF source commit mismatch' }
          python -m pip install --disable-pip-version-check pyinstaller==6.16.0 cryptography==46.0.3 protobuf==5.29.5 bleak==1.1.1
          $provDir = Join-Path $vendor 'network_provisioning/tool/esp_prov'
          $networkProto = Join-Path $vendor 'network_provisioning/python'
          $idfProto = Join-Path $idf 'components/protocomm/python'
          python -m PyInstaller --noconfirm --clean --onefile --name BatteryMonitorEspProv --paths $provDir --add-data "$idfProto;idf/components/protocomm/python" --add-data "$networkProto;python" battery-monitor/windows/esp_provision_helper.py
          $helperDir = 'battery-monitor/windows/staging/esp-provisioner'
          New-Item -ItemType Directory -Force -Path $helperDir | Out-Null
          Copy-Item dist/BatteryMonitorEspProv.exe "$helperDir/BatteryMonitorEspProv.exe"
          Copy-Item "$vendor/json_parser/LICENSE" "$helperDir/APACHE-2.0.txt"

      - name: Build both Windows applications
        shell: pwsh
        run: |
          dotnet restore battery-monitor/windows/BatteryMonitor.Client/BatteryMonitor.Client.csproj
          dotnet restore battery-monitor/windows/BatteryMonitor.FactoryService/BatteryMonitor.FactoryService.csproj
          dotnet build battery-monitor/windows/BatteryMonitor.Client/BatteryMonitor.Client.csproj -c Release --no-restore
          dotnet build battery-monitor/windows/BatteryMonitor.FactoryService/BatteryMonitor.FactoryService.csproj -c Release --no-restore
          dotnet run --project battery-monitor/windows/BatteryMonitor.Client/BatteryMonitor.Client.csproj -c Release --no-build -- --protocol-self-test

      - name: Exercise production firmware verifier
        shell: pwsh
        run: |
          $ErrorActionPreference = 'Stop'
          $dll = (Get-ChildItem battery-monitor/windows/BatteryMonitor.Client/bin/Release -Filter BatteryMonitor.Client.dll -Recurse | Select-Object -First 1).FullName
          if (-not $dll) { throw 'Built BatteryMonitor.Client.dll not found' }
          $asm = [System.Reflection.Assembly]::LoadFrom($dll)
          $type = $asm.GetType('BatteryMonitor.Client.FirmwareSignatureVerifier', $true)
          $method = $type.GetMethod('VerifyOrThrow', [System.Reflection.BindingFlags]'Static,Public,NonPublic')
          if (-not $method) { throw 'FirmwareSignatureVerifier.VerifyOrThrow not found' }

          foreach ($name in @('BatteryMonitor.ino.bin','BatteryMonitor.ino.merged.bin')) {
            $image = (Get-ChildItem battery-monitor/windows/staging/firmware -Filter $name -Recurse | Select-Object -First 1).FullName
            $sig = "$image.sig"
            if (-not (Test-Path $image) -or -not (Test-Path $sig)) { throw "Signed firmware pair missing for $name" }
            try { $null = $method.Invoke($null, @($image, $sig)) }
            catch { throw "Production verifier rejected signed $name`: $($_.Exception.InnerException?.Message ?? $_.Exception.Message)" }

            $tampered = Join-Path $env:RUNNER_TEMP ($name + '.tampered')
            [byte[]]$bytes = [IO.File]::ReadAllBytes($image)
            $bytes[[int]($bytes.Length / 2)] = $bytes[[int]($bytes.Length / 2)] -bxor 1
            [IO.File]::WriteAllBytes($tampered, $bytes)
            $rejected = $false
            try { $null = $method.Invoke($null, @($tampered, $sig)) }
            catch { $rejected = $true }
            if (-not $rejected) { throw "Production verifier accepted tampered $name" }
            Remove-Item $tampered -Force
          }

      - name: Publish separate self-contained win-x64 applications
        shell: pwsh
        run: |
          dotnet publish battery-monitor/windows/BatteryMonitor.Client/BatteryMonitor.Client.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:DebugType=None -o battery-monitor/windows/publish/customer
          dotnet publish battery-monitor/windows/BatteryMonitor.FactoryService/BatteryMonitor.FactoryService.csproj -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -p:IncludeNativeLibrariesForSelfExtract=true -p:DebugType=None -o battery-monitor/windows/publish/factory

      - name: Bundle signed customer and Factory Service packages
        shell: pwsh
        run: |
          $customer = 'battery-monitor/windows/publish/customer'
          $factory = 'battery-monitor/windows/publish/factory'
          New-Item -ItemType Directory -Force -Path "$customer/firmware" | Out-Null
          New-Item -ItemType Directory -Force -Path "$customer/tools/esp-provisioner" | Out-Null
          New-Item -ItemType Directory -Force -Path "$factory/firmware" | Out-Null
          New-Item -ItemType Directory -Force -Path "$factory/tools/esptool" | Out-Null
          New-Item -ItemType Directory -Force -Path "$factory/tools/esp-provisioner" | Out-Null

          foreach ($name in @('BatteryMonitor.ino.bin')) {
            $image = Get-ChildItem battery-monitor/windows/staging/firmware -Filter $name -Recurse | Select-Object -First 1
            if (-not $image -or -not (Test-Path ($image.FullName + '.sig'))) { throw "Missing signed $name pair" }
            Copy-Item $image.FullName (Join-Path "$customer/firmware" $name)
            Copy-Item ($image.FullName + '.sig') (Join-Path "$customer/firmware" ($name + '.sig'))
          }
          Copy-Item battery-monitor/windows/staging/firmware/SIGNED_RELEASE.txt "$customer/firmware/SIGNED_RELEASE.txt"
          Copy-Item battery-monitor/windows/staging/esp-provisioner/* "$customer/tools/esp-provisioner" -Recurse -Force

          foreach ($name in @('BatteryMonitor.ino.bin','BatteryMonitor.ino.merged.bin')) {
            $image = Get-ChildItem battery-monitor/windows/staging/firmware -Filter $name -Recurse | Select-Object -First 1
            if (-not $image -or -not (Test-Path ($image.FullName + '.sig'))) { throw "Missing signed $name pair" }
            Copy-Item $image.FullName (Join-Path "$factory/firmware" $name)
            Copy-Item ($image.FullName + '.sig') (Join-Path "$factory/firmware" ($name + '.sig'))
          }
          Copy-Item battery-monitor/windows/staging/firmware/SIGNED_RELEASE.txt "$factory/firmware/SIGNED_RELEASE.txt"
          Copy-Item battery-monitor/windows/staging/esptool/* "$factory/tools/esptool" -Recurse -Force
          Copy-Item battery-monitor/windows/staging/esp-provisioner/* "$factory/tools/esp-provisioner" -Recurse -Force

          if (Test-Path "$customer/tools/esptool") { throw 'Customer signed package must not contain esptool' }
          if (Test-Path "$customer/firmware/BatteryMonitor.ino.merged.bin") { throw 'Customer signed package must not contain blank-device merged firmware' }

      - name: Upload signed customer package
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: Battery-Monitor-Windows-Customer-Signed-${{ inputs.version }}
          path: battery-monitor/windows/publish/customer

      - name: Upload signed Factory Service package
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: Battery-Monitor-Factory-Service-Signed-${{ inputs.version }}
          path: battery-monitor/windows/publish/factory