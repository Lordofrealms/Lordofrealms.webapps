,...tools].forEach(e=>e.disabled=!on);updateUI()}
function updateUI(){const loaded=!!pdfDoc;saveBtn.disabled=!loaded||!dirty;saveAsBtn.disabled=!loaded;prevBtn.disabled=!loaded||pageNo<=1;nextBtn.disabled=!loaded||pageNo>=Number(pdfDoc?.numPages||1);zoomOutBtn.disabled=!loaded||zoom<=.5;zoomInBtn.disabled=!loaded||zoom>=3;undoBtn.disabled=!history.length;deleteBtn.disabled=!selected;clearBtn.disabled=!loaded||!ann().length;formatBtn.disabled=!loaded;pageLabel.textContent=loaded?`Page ${pageNo} / ${pdfDoc.numPages}`:'Page — / —';zoomLabel.textContent=`${Math.round(zoom*100)}%`;syncFormatPanel()}
function setToggle(btn,state){btn.classList.toggle('active',state===true);btn.classList.toggle('mixed',state==='mixed')}
function aggregateRuns(runs,key){if(!runs?.length)return null;const vals=runs.map(r=>r[key]);return vals.every(v=>v===vals[0])?vals[0]:'mixed'}
function currentPanelRuns(){if(editing){const sr=selectedEditorRuns();if(sr.length)return sr;return editing.a.runs||[]}const a=selectedAnn();return a?.type==='text'?a.runs||[]:[]}
function syncFormatPanel(){const runs=currentPanelRuns();const active=runs.length>0;const source=active?runs:[{...defaultStyle,text:''}];const fam=aggregateRuns(source,'family'),sz=aggregateRuns(source,'size'),col=aggregateRuns(source,'color'),bold=aggregateRuns(source,'bold'),italic=aggregateRuns(source,'italic'),underline=aggregateRuns(source,'underline'),strike=aggregateRuns(source,'strike'),script=aggregateRuns(source,'script');fontFamily.value=fam==='mixed'?'':fam;formatSize.value=sz==='mixed'?'':sz;formatSize.classList.toggle('mixedValue',sz==='mixed');formatColor.value=col==='mixed'?'#000000':col;formatColor.classList.toggle('mixedValue',col==='mixed');setToggle(boldBtn,bold);setToggle(italicBtn,italic);setToggle(underlineBtn,underline);setToggle(strikeBtn,strike);setToggle(superBtn,script==='mixed'?'mixed':script==='super');setToggle(subBtn,script==='mixed'?'mixed':script==='sub');const align=editing?.a?.align||selectedAnn()?.align||defaultAlign;[alignLeftBtn,alignCenterBtn,alignRightBtn].forEach((b,i)=>b.classList.toggle('active',align===['left','center','right'][i]))}
formatBtn.onclick=()=>{formatPanel.classList.toggle('hidden');formatBtn.classList.toggle('active',!formatPanel.classList.contains('hidden'));syncFormatPanel()};
const fsAccess=()=>window.isSecureContext&&'showOpenFilePicker'in window&&'showSaveFilePicker'in window;
openBtn.onclick=async()=>{if(!fsAccess()){fileInput.click();return}try{const[h]=await showOpenFilePicker({multiple:false,types:[{description:'PDF documents',accept:{'application/pdf':['.pdf']}}]});await loadFile(await h.getFile(),h)}catch(e){if(e.name!=='AbortError'){console.error(e);setStatus('Could not open PDF')}}};
fileInput.onchange=async()=>{const f=fileInput.files?.[0];if(f)try{await loadFile(f,null)}catch(e){console.error(e);setStatus('Could not open PDF')}fileInput.value=''};
async function loadFile(file,h){setStatus('Loading PDF…');const bytes=new Uint8Array(await file.arrayBuffer());originalBytes=bytes.slice();fileHandle=h;fileName=file.name||'document.pdf';pdfDoc=await pdfjsLib.getDocument({data:bytes.slice()}).promise;pageNo=1;zoom=1;annotations=new Map();selected=null;history=[];nextId=1;dirty=false;cancelEditor(true);empty.classList.add('hidden');pageShell.classList.remove('hidden');enableEditor(true);await render();setStatus(`${fileName} • ${pdfDoc.numPages} page${pdfDoc.numPages===1?'':'s'} • v${APP_VERSION}`)}
tools.forEach(b=>b.onclick=()=>{commitEditor();tool=b.dataset.tool;tools.forEach(x=>x.classList.toggle('active',x===b));overlayCanvas.style.cursor=tool==='select'?'default':tool==='editPdfText'?'text':'crosshair';if(tool==='text')setStatus('Add Text: click the page and type');if(tool==='editPdfText')setStatus('Edit PDF Text: click an existing text line')});
prevBtn.onclick=async()=>{commitEditor();if(pageNo>1){pageNo--;selected=null;await render()}};nextBtn.onclick=async()=>{commitEditor();if(pageNo<pdfDoc.numPages){pageNo++;selected=null;await render()}};zoomOutBtn.onclick=async()=>{commitEditor();zoom=Math.max(.5,zoom-.25);await render()};zoomInBtn.onclick=async()=>{commitEditor();zoom=Math.min(3,zoom+.25);await render()};
async function render(){if(!pdfDoc)return;const token=++renderToken,page=await pdfDoc.getPage(pageNo);if(token!==renderToken)return;viewport=page.getViewport({scale:zoom});const dpr=Math.min(devicePixelRatio||1,2),rv=page.getViewport({scale:zoom*dpr});pdfCanvas.width=Math.ceil(rv.width);pdfCanvas.height=Math.ceil(rv.height);pdfCanvas.style.width=`${viewport.width}px`;pdfCanvas.style.height=`${viewport.height}px`;overlayCanvas.width=Math.ceil(viewport.width*dpr);overlayCanvas.height=Math.ceil(viewport.height*dpr);overlayCanvas.style.width=`${viewport.width}px`;overlayCanvas.style.height=`${viewport.height}px`;await page.render({canvasContext:pdfCtx,viewport:rv}).promise;if(token!==renderToken)return;await extractPdfText(page);draw();updateUI()}
function inferFont(page,it,styles){const style=styles?.[it.fontName]||{};let obj=null;try{obj=page.commonObjs.get(it.fontName)}catch{}const label=[style.fontFamily,obj?.name,obj?.fallbackName,obj?.loadedName].filter(Boolean).join(' ');let family='Helvetica';if(/courier|mono/i.test(label))family='Courier';else if(/times|serif|roman/i.test(label))family='Times';const bold=/bold|black|heavy|semibold|semi-bold|demi/i.test(label),italic=/italic|oblique/i.test(label);return{family,bold,italic}}
async function extractPdfText(page){try{const tc=await page.getTextContent(),raw=[],W=viewport.width,H=viewport.height;for(const it of tc.items||[]){if(!it?.str?.trim()||!it.transform)continue;const tx=pdfjsLib.Util.transform(viewport.transform,it.transform),angle=Math.atan2(tx[1],tx[0]);if(Math.abs(Math.sin(angle))>.12)continue;const cssH=Math.max(4,Math.hypot(tx[2],tx[3])),cssW=Math.max(2,Math.abs((it.width||0)*viewport.scale)),size=Math.max(4,Math.hypot(it.transform[2],it.transform[3])||it.height||12),xPx=tx[4],baseline=tx[5],top=baseline-cssH,sidePad=Math.max(1.25,cssH*.08),mx=Math.max(0,xPx-sidePad),my=Math.max(0,baseline-cssH*1.15),mr=Math.min(W,xPx+cssW+sidePad),mb=Math.min(H,baseline+cssH*.40),sourceKey=[it.str,Number(it.transform[4]||0).toFixed(3),Number(it.transform[5]||0).toFixed(3),Number(it.width||0).toFixed(3),Number(size||0).toFixed(3)].join('|'),f=inferFont(page,it,tc.styles);raw.push({text:it.str,xPx,baseline,top,cssW,cssH,size,sourceKey,maskRect:{x:mx/W,y:my/H,w:(mr-mx)/W,h:(mb-my)/H},style:{family:f.family,size,bold:f.bold,italic:f.italic,underline:false,strike:false,script:'normal',color:'#000000'}})}pdfTextLines=groupLines(raw,W,H)}catch(e){console.warn(e);pdfTextLines=[]}}
function groupLines(items,W,H){const sorted=[...items].sort((a,b)=>a.baseline-b.baseline||a.xPx-b.xPx),lines=[];for(const it of sorted){let line=lines.find(l=>Math.abs(l.baseline-it.baseline)<=Math.max(2.5,Math.max(l.cssH,it.cssH)*.32));if(!line){line={baseline:it.baseline,cssH:it.cssH,items:[]};lines.push(line)}line.items.push(it);line.cssH=Math.max(line.cssH,it.cssH)}const groups=[];for(const line of lines){line.items.sort((a,b)=>a.xPx-b.xPx);let bucket=[];const flush=()=>{if(!bucket.length)return;const first=bucket[0],last=bucket[bucket.length-1],runs=[],masks=[],keys=[];let prevEnd=first.xPx;for(const item of bucket){const gap=item.xPx-prevEnd;if(runs.length&&gap>Math.max(1,item.cssH*.18)){const spaces=Math.max(1,Math.round(gap/Math.max(3,item.size*.28)));runs.push({text:' '.repeat(Math.min(spaces,8)),...cloneStyle(item.style)})}runs.push({text:item.text,...cloneStyle(item.style)});masks.push({sourceKey:item.sourceKey,rect:structuredClone(item.maskRect),pdfRect:maskPdfRect(item.maskRect),maskColor:sampleMaskColor(item.maskRect)});keys.push(item.sourceKey);prevEnd=item.xPx+item.cssW}const left=first.xPx,right=Math.max(...bucket.map(i=>i.xPx+i.cssW)),top=Math.min(...bucket.map(i=>i.top));groups.push({x:left/W,y:top/H,w:(right-left)/W,h:Math.max(...bucket.map(i=>i.cssH))*1.3/H,runs:normalizeRuns(runs),sourceMasks:masks,sourceKeys:keys,align:'left'});bucket=[]};for(const it of line.items){if(bucket.length){const prev=bucket[bucket.length-1],gap=it.xPx-(prev.xPx+prev.cssW);if(gap>Math.max(28,line.cssH*3.2))flush()}bucket.push(it)}flush()}return groups}
function maskPdfRect(r){const x1=r.x*viewport.width,y1=r.y*viewport.height,x2=(r.x+r.w)*viewport.width,y2=(r.y+r.h)*viewport.height,pts=[viewport.convertToPdfPoint(x1,y1),viewport.convertToPdfPoint(x2,y1),viewport.convertToPdfPoint(x1,y2),viewport.convertToPdfPoint(x2,y2)],xs=pts.map(p=>p[0]),ys=pts.map(p=>p[1]);return{x:Math.min(...xs),y:Math.min(...ys),w:Math.max(...xs)-Math.min(...xs),h:Math.max(...ys)-Math.min(...ys)}}
function sampleMaskColor(r){
try{
const sx=pdfCanvas.width/viewport.width,sy=pdfCanvas.height/viewport.height,W=viewport.width,H=viewport.height;
const left=r.x*W,right=(r.x+r.w)*W,top=r.y*H,bottom=(r.y+r.h)*H;
const pad=Math.max(3,Math.min(12,(bottom-top)*.45)),samples=[];
const add=(cx,cy)=>{const px=Math.max(0,Math.min(pdfCanvas.width-1,Math.round(cx*sx))),py=Math.max(0,Math.min(pdfCanvas.height-1,Math.round(cy*sy))),d=pdfCtx.getImageData(px,py,1,1).data,rgb=[d[0],d[1],d[2]],mx=Math.max(...rgb),mn=Math.min(...rgb),lum=.2126*rgb[0]+.7152*rgb[1]+.0722*rgb[2];if(lum>=165&&mx-mn<=75)samples.push(rgb)};
const steps=14;
for(let i=0;i<=steps;i++){const t=i/steps,x=left+(right-left)*t,y=top+(bottom-top)*t;add(x,top-pad);add(x,bottom+pad);add(left-pad,y);add(right+pad,y);add(x,top-pad*.55);add(x,bottom+pad*.55)}
if(!samples.length)return'#ffffff';
const buckets=new Map();
for(const rgb of samples){const q=rgb.map(v=>Math.max(0,Math.min(255,Math.round(v/12)*12))),key=q.join(','),b=buckets.get(key)||{count:0,sum:[0,0,0]};b.count++;b.sum[0]+=rgb[0];b.sum[1]+=rgb[1];b.sum[2]+=rgb[2];buckets.set(key,b)}
let best=null;for(const b of buckets.values())if(!best||b.count>best.count)best=b;
let rgb=best.sum.map(v=>Math.round(v/best.count));const mx=Math.max(...rgb),mn=Math.min(...rgb);if(mn>=244&&mx-mn<=12)rgb=[255,255,255];return'#'+rgb.map(v=>v.toString(16).padStart(2,'0')).join('')
}catch{return'#ffffff'}
}
function point(e){const r=overlayCanvas.getBoundingClientRect();return{x:Math.max(0,Math.min(1,(e.clientX-r.left)/r.width)),y:Math.max(0,Math.min(1,(e.clientY-r.top)/r.height))}}
function canvasFont(s){return`${s.italic?'italic ':''}${s.bold?'700 ':'400 '}${Math.max(1,s.size*zoom)}px ${s.family=