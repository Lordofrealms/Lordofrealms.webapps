function bounds(a){const W=viewport.width,H=viewport.height;if(a.type==='text'){const maxSize=Math.max(4,...(a.runs||[]).map(r=>r.size));return{x:a.x,y:Math.max(0,a.y-maxSize*zoom*.38/H),w:Math.max(textBoxWidthPx(a)/W,.02),h:Math.max(maxSize*zoom*1.65/H,.02)}}if(a.type==='whiteout'){if(a.sourceMasks?.length){const xs=a.sourceMasks.flatMap(m=>[m.rect.x,m.rect.x+m.rect.w]),ys=a.sourceMasks.flatMap(m=>[m.rect.y,m.rect.y+m.rect.h]);return{x:Math.min(...xs),y:Math.min(...ys),w:Math.max(...xs)-Math.min(...xs),h:Math.max(...ys)-Math.min(...ys)}}return{x:a.x,y:a.y,w:a.w,h:a.h}}if(a.type==='pen'){const xs=a.points.map(p=>p.x),ys=a.points.map(p=>p.y),pad=Math.max(.008,a.width*zoom/Math.min(W,H));return{x:Math.min(...xs)-pad,y:Math.min(...ys)-pad,w:Math.max(...xs)-Math.min(...xs)+2*pad,h:Math.max(...ys)-Math.min(...ys)+2*pad}}}
function hit(p){const list=ann();for(let i=list.length-1;i>=0;i--){const b=bounds(list[i]),pad=.008;if(p.x>=b.x-pad&&p.x<=b.x+b.w+pad&&p.y>=b.y-pad&&p.y<=b.y+b.h+pad)return list[i]}return null}
function sourceConsumed(keys){const used=new Set();for(const a of ann())for(const k of a.sourceKeys||[])used.add(k);return keys.some(k=>used.has(k))}
function hitPdfLine(p){for(let i=pdfTextLines.length-1;i>=0;i--){const t=pdfTextLines[i];if(sourceConsumed(t.sourceKeys))continue;const pad=.008;if(p.x>=t.x-pad&&p.x<=t.x+t.w+pad&&p.y>=t.y-pad&&p.y<=t.y+t.h+pad)return t}return null}
function spanStyle(r){const fam=r.family==='Times'?'Times New Roman,serif':r.family==='Courier'?'Courier New,monospace':'Arial,Helvetica,sans-serif';const size=r.size*zoom;return`font-family:${fam};font-size:${size}px;font-weight:${r.bold?'700':'400'};font-style:${r.italic?'italic':'normal'};text-decoration:${[r.underline?'underline':'',r.strike?'line-through':''].filter(Boolean).join(' ')||'none'};vertical-align:${r.script==='super'?'super':r.script==='sub'?'sub':'baseline'};color:${r.color};`}
function renderEditorRuns(a){inlineEditor.innerHTML='';for(const r of a.runs||[]){const s=document.createElement('span');s.textContent=r.text;s.setAttribute('style',spanStyle(r));inlineEditor.appendChild(s)}if(!inlineEditor.childNodes.length){const s=document.createElement('span');s.textContent='';s.setAttribute('style',spanStyle(defaultStyle));inlineEditor.appendChild(s)}}
function startEditor(a,isNew,before){if(!a||a.type!=='text')return;editing={a,isNew,before:before||snapshot()};selected={page:pageNo,id:a.id};renderEditorRuns(a);positionEditor();inlineEditor.style.display='block';formatPanel.classList.remove('hidden');formatBtn.classList.add('active');draw();setTimeout(()=>{if(editing?.a.id===a.id){inlineEditor.focus({preventScroll:true});const sel=getSelection(),range=document.createRange();range.selectNodeContents(inlineEditor);if(!isNew){sel.removeAllRanges();sel.addRange(range)}else{range.collapse(false);sel.removeAllRanges();sel.addRange(range)}syncFormatPanel()}},0)}
function positionEditor(){if(!editing||!viewport)return;const a=editing.a,left=a.x*viewport.width,top=a.y*viewport.height;inlineEditor.style.left=`${left}px`;inlineEditor.style.top=`${top}px`;inlineEditor.style.minWidth=`${Math.max(100,textBoxWidthPx(a)+24)}px`;inlineEditor.style.textAlign=a.align||'left'}
function elementStyle(el){const cs=getComputedStyle(el),family=/courier|mono/i.test(cs.fontFamily)?'Courier':/times|serif/i.test(cs.fontFamily)?'Times':'Helvetica',weight=parseInt(cs.fontWeight,10);return{family,size:Math.max(4,(parseFloat(cs.fontSize)||16)/zoom),bold:Number.isFinite(weight)?weight>=600:/bold/i.test(cs.fontWeight),italic:cs.fontStyle!=='normal',underline:(cs.textDecorationLine||'').includes('underline'),strike:(cs.textDecorationLine||'').includes('line-through'),script:cs.verticalAlign==='super'?'super':cs.verticalAlign==='sub'?'sub':'normal',color:cssColorToHex(cs.color)}}
function editorToRuns(){const out=[];const walk=n=>{if(n.nodeType===Node.TEXT_NODE){if(n.nodeValue){const e=n.parentElement||inlineEditor;out.push({text:n.nodeValue,...elementStyle(e)})}}else if(n.nodeType===Node.ELEMENT_NODE){if(n.tagName==='BR')out.push({text:' ',...cloneStyle(defaultStyle)});else [...n.childNodes].forEach(walk)}};[...inlineEditor.childNodes].forEach(walk);return normalizeRuns(out)}
function commitEditor(){if(!editing)return;const{a,before}=editing;a.runs=editorToRuns();inlineEditor.style.display='none';editing=null;if(!annotationText(a).trim()){const list=ann(),i=list.findIndex(x=>x.id===a.id);if(i>=0){if(a.sourceMasks?.length)list.splice(i,1,{id:nextId++,type:'whiteout',sourceMasks:structuredClone(a.sourceMasks),sourceKeys:[...(a.sourceKeys||[])]});else list.splice(i,1)}selected=null}else selected={page:pageNo,id:a.id};if(JSON.stringify(before)!==JSON.stringify(ann()))pushHistory(before);draw();updateUI()}
function cancelEditor(silent=false){if(!editing){inlineEditor.style.display='none';return}annotations.set(pageNo,structuredClone(editing.before));editing=null;inlineEditor.style.display='none';selected=null;if(!silent){draw();updateUI()}}
inlineEditor.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();commitEditor()}else if(e.key==='Escape'){e.preventDefault();cancelEditor()}});inlineEditor.addEventListener('input',()=>{if(editing){editing.a.runs=editorToRuns();positionEditor();draw();syncFormatPanel()}});document.addEventListener('selectionchange',()=>{if(editing&&document.activeElement===inlineEditor)syncFormatPanel()});
function editorSelectionRange(){const s=getSelection();if(!editing||!s||!s.rangeCount)return null;const r=s.getRangeAt(0);if(!inlineEditor.contains(r.commonAncestorContainer))return null;return r}
function selectedEditorRuns(){const r=editorSelectionRange();if(!r||r.collapsed)return[];const saved=r.cloneRange(),frag=saved.cloneContents(),box=document.createElement('div');box.appendChild(frag);if(!box.textContent)return[];document.body.appendChild(box);box.style.position='fixed';box.style.left='-10000px';const runs=[];const walk=n=>{if(n.nodeType===Node.TEXT_NODE&&n.nodeValue)runs.push({text:n.nodeValue,...elementStyle(n.parentElement||box)});else if(n.nodeType===Node.ELEMENT_NODE)[...n.childNodes].forEach(walk)};[...box.childNodes].forEach(walk);box.remove();return normalizeRuns(runs)}
function applyToEditorSelection(stylePatch){const r=editorSelectionRange();if(!r)return false;if(r.collapsed){let el=r.startContainer.nodeType===Node.ELEMENT_NODE?r.startContainer:r.startContainer.parentElement;if(el===inlineEditor){const s=document.createElement('span');s.textContent='\u200b';const base={...defaultStyle,...stylePatch};s.setAttribute('style',spanStyle(base));r.insertNode(s);const nr=document.createRange();nr.setStart(s.firstChild,1);nr.collapse(true);const sel=getSelection();sel.removeAllRanges();sel.addRange(nr)}else Object.assign(el.style,patchToCss(stylePatch));return true}const frag=r.extractContents(),span=document.createElement('span');span.appendChild(frag);Object.assign(span.style,patchToCss(stylePatch));r.insertNode(span);const nr=document.createRange();nr.selectNodeContents(span);const sel=getSelection();sel.removeAllRanges();sel.addRange(nr);inlineEditor.dispatchEvent(new Event('input'));return true}
function patchToCss(p){const css={};if('family'in p)css.fontFamily=p.family==='Times'?'Times New Roman, serif':p.family==='Courier'?'Courier New, monospace':'Arial, Helvetica, sans-serif';if('size'in p)css.fontSize=`${p.size*zoom}px`;if('bold'in p)css.fontWeight=p.bold?'700':'400';if('italic'in p)css.fontStyle=p.italic?'italic':'normal';if('underline'in p||'strike'in p){const current=[];if(p.underline)current.push('underline');if(p.strike)current.push('line-through');css.textDecoration=current.join(' ')||'none'}if('script'in p)css.verticalAlign=p.script==='super'?'super':p.script==='sub'?'sub':'baseline';if('color'in p)css.color=p.color;return css}
function applyPatchToAnnotation(a,patch){for(const r of a.runs||[])Object.assign(r,patch);a.runs=normalizeRuns(a.runs)}
function doFormat(patch){const before=snapshot();if(editing){if(!applyToEditorSelection(patch)){applyPatchToAnnotation(editing.a,patch);renderEditorRuns(editing.a)}editing.a.runs=editorToRuns();positionEditor()}else{const a=selectedAnn();if(a?.type==='text')applyPatchToAnnotation(a,patch);else Object.assign(defaultStyle,patch)}if(JSON.stringify(before)!==JSON.stringify(ann()))pushHistory(before);draw();syncFormatPanel()}
function toggleFormat(key){const runs=currentPanelRuns(),state=aggregateRuns(runs.length?runs:[{...defaultStyle,text:''}],key),desired=state==='mixed'?true:!state;doFormat({[key]:desired})}
boldBtn.onclick=()=>toggleFormat('bold');italicBtn.onclick=()=>toggleFormat('italic');underlineBtn.onclick=()=>toggleFormat('underline');strikeBtn.onclick=()=>toggleFormat('strike');superBtn.onclick=()=>{const runs=currentPanelRuns(),state=aggregateRuns(runs.length?runs:[{...defaultStyle,text:''}],'script');doFormat({script:state==='super'?'normal':'super'})};subBtn.onclick=()=>{const runs=currentPanelRuns(),state=aggregateRuns(runs.length?runs:[{...defaultStyle,text:''}],'script');doFormat({script:state==='sub'?'normal':'sub'})};
fontFamily.onchange=()=>{if(fontFamily.value)doFormat({family:fontFamily.value})};formatSize.onchange=()=>{const v=+formatSize.value;if(v>=4&&v<=144)doFormat({size:v})};formatColor.oninput=()=>doFormat({color:formatColor.value});
function setAlign(v){if(editing){const before=snapshot();editing.a.align=v;positionEditor();if(JSON.stringify(before)!==JSON.stringify(ann()))pushHistory(before)}else{const a=selectedAnn();if(a?.type==='text'){const before=snapshot();a.align=v;pushHistory(before)}else defaultAlign=v}draw();syncFormatPanel()}
alignLeftBtn.onclick=()=>setAlign('left');alignCenterBtn.onclick=()=>setAlign('center');alignRightBtn.onclick=()=>setAlign('right');
overlayCanvas.onpointerdown=e=>{if(!pdfDoc)return;if(editing)commitEditor();const p=point(e);if(tool==='text'){const before=snapshot(),a={id:nextId++,type:'text',x:p.x,y:p.y,runs:[{text:'',...cloneStyle(defaultStyle)}],align:defaultAlign,boxWidth:0,sourceMasks:[],sourceKeys:[]};ann().push(a);startEditor(a,true,before);return}if(tool==='editPdfText'){const editable=hit(p);if(editable?.type==='text'){startEditor(editable,false,snapshot());return}if(editable?.type==='whiteout'&&editable.sourceKeys?.length){setStatus('That original PDF text is deleted. Use Undo to restore it.');return}const src=hitPdfLine(p);if(!src){setStatus('No selectable PDF text line found there. Scanned/image text needs OCR.');return}const before=snapshot(),a={id:nextId++,type:'text',x:src.x,y:src.y,runs:structuredClone(src.runs),align:src.align||'left',boxWidth:src.w,sourceMasks:structuredClone(src.sourceMasks),sourceKeys:[...src.sourceKeys]};ann().push(a);startEditor(a,false,before);return}overlayCanvas.setPointerCapture(e.pointerId);if(tool==='pen'){const before=snapshot(),a={id:nextId++,type:'pen',points:[p],width:+penWidth.value||3,color:penColor.value};ann().push(a);drawing={kind:'pen',a,before}}else if(tool==='whiteout'){const before=snapshot(),a={id:nextId++,type:'whiteout',x:p.x,y:p.y,w:0,h:0,sx:p.x,sy:p.y,maskColor:'#ffffff'};ann().push(a);drawing={kind:'whiteout',a,before}}else if(tool==='select'){const a=hit(p);selected=a?{page:pageNo,id:a.id}:null;if(a)moving={a,start:p,before:snapshot(),changed:false};draw();updateUI()}};
overlayCanvas.ondblclick=e=>{if(!pdfDoc||tool!=='select')return;const a=hit(point(e));if(a?.type==='text'){e.preventDefault();moving=null;startEditor(a,false,snapshot())}};
